apiVersion: v1
kind: Service
metadata:
  name: kerberos-webapp-ui
  labels:
    app.kubernetes.io/name: kerberos-webapp
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kerberos-webapp
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
