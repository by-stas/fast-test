apiVersion: v1
kind: Service
metadata:
  name: kerberos-webapp-api
  labels:
    app.kubernetes.io/name: kerberos-webapp
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kerberos-webapp
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
